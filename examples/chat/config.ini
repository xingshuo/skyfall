[app]
thread = 4
module_path = examples/chat/?.so
bootstrap = testchat bootstrap ; service_name args

[log]
; 日志文件，留空或填 stdout 表示输出到标准输出
logfile = stdout
; 日志等级 { DEBUG, INFO, WARN, ERROR }
loglevel = DEBUG